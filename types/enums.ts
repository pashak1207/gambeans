export enum Users_role {
    ADMIN = "ADMIN",
    SUPERADMIN ="SUPERADMIN",
    USER = "USER"
}

export enum User_status {
    ACTIVE = "ACTIVE",
    BLOCKED = "BLOCKED",
}

export enum Prize_type {
    SCRATCH = "SCRATCH",
    SLOT = "SLOT",
    FREE = "FREE",
    FIRST = "FIRST",
}

export enum Env_version {
    he = "he",
    en = "en",
}

export enum Sort_method{
    ASC="asc",
    DESC="desc"
}